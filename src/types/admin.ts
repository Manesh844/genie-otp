
export interface ApiConfig {
  id: string;
  name: string;
  url: string;
  apiKey: string;
}

export interface CountryConfig {
  id: string;
  apiId: string;
  apiName: string; 
  name: string;
  code: string; 
}
